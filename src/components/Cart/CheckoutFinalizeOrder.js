import React from 'react';

const FinalizeOrder = ({title}) => {
  return(
    <div>
      <div>{title}</div>
      <div>
        user details
      </div>
    </div>
  )
}

export default FinalizeOrder;